/* ClaudeAmp — single version source for everything user-visible.
   Keep in lockstep with package.json "version"; the release workflow
   fails the build if the two ever disagree. */
"use strict";
window.CLAUDEAMP_VERSION = "1.7.6";
